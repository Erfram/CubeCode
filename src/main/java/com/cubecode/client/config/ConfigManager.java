package com.cubecode.client.config;

import com.cubecode.api.utils.DirectoryManager;

import java.io.File;

public class ConfigManager extends DirectoryManager {
    public ConfigManager(File directory) {
        super(directory);
    }
}