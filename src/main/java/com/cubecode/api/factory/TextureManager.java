package com.cubecode.api.factory;

import com.cubecode.api.utils.DirectoryManager;

import java.io.File;
public class TextureManager extends DirectoryManager {

    public TextureManager(File directory) {
        super(directory);
    }
}